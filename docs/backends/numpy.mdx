# NumPy Backend

`NumpyBackend` is the compatibility baseline and the default target for most local environments.

Highlights:

- native NumPy arrays and math
- optional SciPy acceleration for selected special/statistical functions
- deterministic key behavior through NumPy seeding
- CPU-only device semantics

Use this backend as your reference correctness backend when comparing JAX/Torch behavior.
